# Space

The `Space` widget provides an empty space for layout purposes. It derives from `Widget` and is used to add spacing between other widgets in containers.

## Class Hierarchy

```
Object
└── Widget
    └── Space
```

## Methods

This widget has no additional methods beyond those inherited from `Widget`.

## Properties

This widget has no additional properties beyond those inherited from `Widget`.

## Events

This widget has no events beyond those inherited from `Widget`.
